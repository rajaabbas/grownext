# Prisma reads this file when running commands from packages/db/.
# Copy to packages/db/.env and set your own DATABASE_URL (not committed).

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:55432/postgres
